/*
 * AttendanceList Messages
 *
 * This contains all the text for the AttendanceList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AttendanceList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AttendanceList container!',
  },
});
