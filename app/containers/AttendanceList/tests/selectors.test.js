// import { selectAttendanceListDomain } from '../selectors';

describe('selectAttendanceListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
