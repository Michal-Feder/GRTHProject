// import { selectAttendanceRegistrationDomain } from '../selectors';

describe('selectAttendanceRegistrationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
